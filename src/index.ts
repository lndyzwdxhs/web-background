// web-background node half
//
// The background feature is browser-only. The node entry exists to satisfy
// the bundle-client package shape and is intentionally a no-op loader entry,
// matching the shipped ui-* packages.

export const inject = []

export function apply() {}
